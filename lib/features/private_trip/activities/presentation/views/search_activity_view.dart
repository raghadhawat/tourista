import 'package:flutter/material.dart';

class SearchActivityView extends StatelessWidget {
  const SearchActivityView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
        child: Scaffold(
      body: SizedBox(),
    ));
  }
}
